@extends('layouts.app')

@section('title', 'Migration insights | Admin')

@section('content')
<section class="min-h-[70vh] bg-slate-50 py-10 sm:py-14"><div class="mx-auto max-w-7xl px-5 sm:px-6 lg:px-8"><div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="text-sm font-bold uppercase tracking-[0.16em] text-sky-700">Content studio</p><h1 class="mt-2 text-3xl font-bold tracking-tight text-slate-950">Migration insights</h1><p class="mt-2 text-sm text-slate-600">Create and edit the articles displayed on the public insights pages.</p></div><div class="flex gap-3"><a href="{{ route('admin.content.index') }}" class="rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm font-bold text-slate-600">Page content</a><a href="{{ route('admin.posts.create') }}" class="rounded-xl bg-slate-950 px-4 py-2.5 text-sm font-bold text-white transition hover:bg-sky-700">New insight</a></div></div>@if (session('success'))<div class="mt-7 rounded-2xl border border-emerald-200 bg-emerald-50 px-5 py-4 text-sm font-semibold text-emerald-800">{{ session('success') }}</div>@endif<div class="mt-8 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-[780px] w-full text-left text-sm"><thead class="bg-slate-50 text-xs font-bold uppercase tracking-[0.08em] text-slate-500"><tr><th class="px-5 py-4">Article</th><th class="px-5 py-4">Category</th><th class="px-5 py-4">Published</th><th class="px-5 py-4">Status</th><th class="px-5 py-4">Action</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($posts as $post)<tr><td class="px-5 py-4"><p class="font-bold text-slate-900">{{ $post->title }}</p><p class="mt-1 text-xs text-slate-500">/{{ $post->slug }}</p></td><td class="px-5 py-4 text-slate-600">{{ $post->category }}</td><td class="px-5 py-4 text-slate-600">{{ $post->published_at?->format('d M Y, H:i') ?? 'Not scheduled' }}</td><td class="px-5 py-4"><span class="rounded-full px-2.5 py-1 text-xs font-bold {{ $post->published_at?->isPast() ? 'bg-emerald-50 text-emerald-700' : 'bg-amber-50 text-amber-700' }}">{{ $post->published_at?->isPast() ? 'Live' : 'Draft / scheduled' }}</span></td><td class="px-5 py-4"><div class="flex gap-2"><a href="{{ route('admin.posts.edit', $post) }}" class="rounded-lg bg-sky-50 px-3 py-2 text-xs font-bold text-sky-700">Edit</a><form action="{{ route('admin.posts.destroy', $post) }}" method="POST">@csrf @method('DELETE')<button class="rounded-lg border border-rose-200 px-3 py-2 text-xs font-bold text-rose-600">Delete</button></form></div></td></tr>@empty<tr><td colspan="5" class="px-5 py-12 text-center text-sm text-slate-500">No insights yet. Create the first article.</td></tr>@endforelse</tbody></table></div></div></div></section>
@endsection
